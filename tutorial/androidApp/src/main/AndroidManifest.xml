<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tutorial.androidApp">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <application
        android:allowBackup="false"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.auth.LoginActivity"
            android:label="@string/app_name"/>
        <activity
            android:name=".ui.onboarding.LandingActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.onboarding.SelectTrainingActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.onboarding.WalkThroughActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.main.HomeActivity"
            android:label="@string/title_activity_home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>